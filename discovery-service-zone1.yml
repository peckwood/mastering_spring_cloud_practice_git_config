eureka:
  instance:
    hostname: localhost
    metadataMap:
      zone: zone1
  client:
    serviceUrl:
      # url of other discover services
      defaultZone: http://localhost:8762/eureka/,http://localhost:8763/eureka/
server:
  port: ${PORT:8761}